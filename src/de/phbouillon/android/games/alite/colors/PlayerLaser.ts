import { AliteColor } from "./AliteColor";

export class PlayerLaser {
  public static diffuse = AliteColor.argb(1.0, 0.8, 0.2, 0.2);
  public static specular = AliteColor.argb(1.0, 1.0, 0.2, 0.2);
}
